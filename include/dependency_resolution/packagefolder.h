#pragma once

namespace cget {
    namespace dependency_resolution {
        // Package folder view from a dependency resolution perspective that allows for writes to a certain
        // location inside the package folder
        class PackageFolder {

        };
    }
}